// Code generated by trpcgo. DO NOT EDIT.

import { z } from "zod";

export const EmailSchema = z.string().meta({ id: "Email" });

export const RoleSchema = z.enum(["admin", "editor", "viewer"]).meta({ id: "Role" });

export const CreateUserInputSchema = z.object({
  name: z.string().min(1).max(100).describe("The user's display name."),
  email: z.email().describe("The user's email address."),
  role: z.enum(["admin", "editor", "viewer"]).or(z.literal("")).optional().describe("The role to assign. Defaults to \"viewer\" on the server."),
  bio: z.string().max(500).or(z.literal("")).optional().describe("Optional biography."),
}).meta({ id: "CreateUserInput" });

export const GetUserByIdInputSchema = z.object({
  id: z.string(),
}).meta({ id: "GetUserByIdInput" });

export const ListUsersInputSchema = z.object({
  page: z.int().gte(1),
  perPage: z.int().gte(1).lte(100),
}).meta({ id: "ListUsersInput" });

